const obj = {
    js: 'java script',
    cpp: "c++",
    java: "java",
    py:"python"
}
for (const key in obj) {
    // console.log(`${key} shortcuts is for ${obj[key]} `);
    
}

const array = [  "js", 'ruby','cpp', 'python']

for (const key in array) {

//it give keys.

   console.log(`${array[key]}`)
   console.log(`${key}`)
}

